const formStatusApplication = document.querySelector("#form-status-application")
if (formStatusApplication) {
    formStatusApplication.addEventListener("submit", (e) => {
        e.preventDefault()
        // console.log(url.href);
        // console.log(status);
        // console.log(id);
    })
}