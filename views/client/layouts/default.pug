include ../partials/header.pug
include ../partials/footer.pug

doctype html
html(lang="vi")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1")
    title #{pageTitle || "Website tuyển dụng"}

    // Bootstrap cơ bản
    link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet")
    link(rel="preconnect" href="https://fonts.googleapis.com")
    link(rel="preconnect" href="https://fonts.gstatic.com" crossorigin)
    link(href="https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:wght@400;500;600;700&display=swap" rel="stylesheet")
    link(rel="stylesheet" href="/css/style.css")
    block styles
    link(
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
  )

  body
    +header()
    .main
      //- .sidebar
      //-   form(method="GET" action="/jobs")

      //-     h3.form-title Điều hướng

      //-     label.form-label Địa điểm
      //-     select.form-control(name="location")
      //-       option(value="") Tất cả
      //-       option(value="Hà Nội") Hà Nội
      //-       option(value="TP.HCM") TP.HCM

      //-     label.form-label Loại việc
      //-     select.form-control(name="type")
      //-       option(value="") Tất cả
      //-       option(value="Full-time") Full-time
      //-       option(value="Remote") Remote

      //-     label.form-label Sắp xếp
      //-     select.form-control(name="sort")
      //-       option(value="new") Mới nhất
      //-       option(value="salary") Lương cao

      //-     button(type="submit") Áp dụng
      block main

    +footer()
    script(src="/js/script.js") 
